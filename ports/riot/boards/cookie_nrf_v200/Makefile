# RIOT board: Cookie nRF V2.00 (CEI-UPM). Out-of-tree board, selected with
# BOARD=cookie_nrf_v200 and EXTERNAL_BOARD_DIRS pointing at ports/riot/boards.
MODULE = board

include $(RIOTBASE)/Makefile.base

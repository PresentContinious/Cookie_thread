# Enables the measurement LED aid (green 5x at boot, red pulse per SED wake as
# an oscilloscope trigger). Add after profile_sed.conf + measure.conf in the
# EXTRA_CONF_FILE list. See CONFIG_NODE_MEAS_LED help.
CONFIG_NODE_MEAS_LED=y
